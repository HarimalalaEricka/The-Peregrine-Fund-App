<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions nécessaires -->
    <uses-permission android:name="android.permission.SMS_SENDER" />  <!-- ESSENTIEL pour les requêtes SMTP -->
    <uses-permission android:name="android.permission.INTERNET" />  <!-- ESSENTIEL pour les requêtes SMTP -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Recommandé -->
    <!-- <uses-permission android:name="android.permission.SEND_SMS"/> --> <!-- (Optionnel si SMS non utilisé) -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThePeregrineFund"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">  <!-- Améliore le clavier pour EditText -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>