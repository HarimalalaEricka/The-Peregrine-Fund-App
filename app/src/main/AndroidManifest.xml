<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>



 
    
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThePeregrineFund"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Activité de connexion (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <!-- Activité principale (si besoin plus tard) -->
        <!-- <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait" /> -->

        <!-- Dashboard -->
        <activity
            android:name=".BaseActivity"
            android:exported="false"
            android:theme="@style/Theme.ThePeregrineFund.NoActionBar"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.ThePeregrineFund.NoActionBar"
            android:screenOrientation="portrait" />

        <activity
            android:name=".SendAlerteActivity"
            android:exported="false"
            android:theme="@style/Theme.ThePeregrineFund.NoActionBar"
            android:screenOrientation="portrait" />
        
        <activity
            android:name=".StatActivity"
            android:exported="false"
            android:theme="@style/Theme.ThePeregrineFund.NoActionBar"
            android:screenOrientation="portrait" />

        <activity
            android:name=".DbDebugActivity"
            android:exported="false"
            android:theme="@style/Theme.ThePeregrineFund.NoActionBar"
            android:screenOrientation="portrait" />

    </application>

</manifest>